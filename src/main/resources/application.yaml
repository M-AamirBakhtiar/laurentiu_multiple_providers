spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security_test_db
    username: security_app
    password: security_app123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org:
      springframework:
        security: DEBUG
